# Results

This directory contains sanitized reference artifacts for the paper-facing analyses.
Machine-local paths and non-portable JSON values have been removed for publication.

The governed release surface is under `sae_writeback_limitation_release/`; its
manifest is emitted with the public tables at
`../tables/sae_writeback_limitation_release/release_manifest.json`.
